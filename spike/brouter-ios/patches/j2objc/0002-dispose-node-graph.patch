diff --git a/brouter-core/src/main/java/btools/router/RoutingEngine.java b/brouter-core/src/main/java/btools/router/RoutingEngine.java
index 1ac94aaf..14a85be4 100644
--- a/brouter-core/src/main/java/btools/router/RoutingEngine.java
+++ b/brouter-core/src/main/java/btools/router/RoutingEngine.java
@@ -335,6 +335,7 @@ public class RoutingEngine extends Thread {
         if (hasInfo() && nodesCache != null) {
           logInfo("NodesCache status before close=" + nodesCache.formatStatus());
         }
+        nodesCache.nodesMap.dispose();
         nodesCache.close();
         nodesCache = null;
       }
@@ -835,6 +836,7 @@ public class RoutingEngine extends Thread {
     } finally {
       ProfileCache.releaseProfile(routingContext);
       if (nodesCache != null) {
+        nodesCache.nodesMap.dispose();
         nodesCache.close();
         nodesCache = null;
       }
@@ -1811,6 +1813,10 @@ public class RoutingEngine extends Thread {
     }
     long maxmem = routingContext.memoryclass * 1024L * 1024L; // in MB
 
+    if (nodesCache != null) {
+      // the next cache re-uses the old one's files, never its graph
+      nodesCache.nodesMap.dispose();
+    }
     nodesCache = new NodesCache(segmentDir, routingContext.expctxWay, routingContext.forceSecondaryData, maxmem, nodesCache, detailed);
     islandNodePairs.clearTempPairs();
   }
diff --git a/brouter-mapaccess/src/main/java/btools/mapaccess/OsmLink.java b/brouter-mapaccess/src/main/java/btools/mapaccess/OsmLink.java
index 963765ee..b1cd61b1 100644
--- a/brouter-mapaccess/src/main/java/btools/mapaccess/OsmLink.java
+++ b/brouter-mapaccess/src/main/java/btools/mapaccess/OsmLink.java
@@ -36,6 +36,20 @@ public class OsmLink {
     n2 = target;
   }
 
+  /**
+   * Drops every reference this link holds (see OsmNodesMap.dispose)
+   */
+  final void dispose() {
+    n1 = null;
+    n2 = null;
+    previous = null;
+    next = null;
+    reverselinkholder = null;
+    firstlinkholder = null;
+    descriptionBitmap = null;
+    geometry = null;
+  }
+
   /**
    * Get the relevant target-node for the given source
    */
diff --git a/brouter-mapaccess/src/main/java/btools/mapaccess/OsmNodesMap.java b/brouter-mapaccess/src/main/java/btools/mapaccess/OsmNodesMap.java
index 83863d82..0e8a2228 100644
--- a/brouter-mapaccess/src/main/java/btools/mapaccess/OsmNodesMap.java
+++ b/brouter-mapaccess/src/main/java/btools/mapaccess/OsmNodesMap.java
@@ -14,6 +14,8 @@ import btools.util.ByteArrayUnifier;
 
 public final class OsmNodesMap {
   private Map<OsmNode, OsmNode> hmap = new HashMap<>(4096);
+  // every node ever put, including those the search later removed or vanished: see dispose()
+  private List<OsmNode> created = new ArrayList<>(4096);
 
   private ByteArrayUnifier abUnifier = new ByteArrayUnifier(16384, false);
 
@@ -175,6 +177,47 @@ public final class OsmNodesMap {
     nodes2check = null;
   }
 
+  /**
+   * Takes the node/link graph apart once a search is done with it.
+   *
+   * On the JVM dropping the map is enough. Under reference counting (J2ObjC) nodes and links point
+   * at each other, so the graph is never freed and every route leaks it. The walk collects first and
+   * clears afterwards, because a link sits in the link chains of both its nodes.
+   */
+  public void dispose() {
+    java.util.Set<OsmNode> nodes = java.util.Collections.newSetFromMap(new java.util.IdentityHashMap<OsmNode, Boolean>());
+    java.util.Set<OsmLink> links = java.util.Collections.newSetFromMap(new java.util.IdentityHashMap<OsmLink, Boolean>());
+    // Seeded from every node ever put, not just the map: nodes the search vanished were removed
+    // from it but can still hold a link to a hollow node, and nothing reachable points back.
+    List<OsmNode> stack = new ArrayList<>(created);
+    stack.addAll(hmap.values());
+    while (!stack.isEmpty()) {
+      OsmNode n = stack.remove(stack.size() - 1);
+      if (!nodes.add(n)) {
+        continue;
+      }
+      for (OsmLink l = n.firstlink; l != null; l = l.getNext(n)) {
+        links.add(l);
+        OsmNode t = l.getTarget(n);
+        if (t != null && !nodes.contains(t)) {
+          stack.add(t);
+        }
+      }
+    }
+    for (OsmLink l : links) {
+      l.dispose();
+    }
+    for (OsmNode n : nodes) {
+      n.firstlink = null;
+    }
+    hmap.clear();
+    created.clear();
+    nodes2check = null;
+    destination = null;
+    endNode1 = null;
+    endNode2 = null;
+  }
+
   public void collectOutreachers() {
     nodes2check = new ArrayList<>(nodesCreated);
     nodesCreated = 0;
@@ -236,6 +279,7 @@ public final class OsmNodesMap {
    * @return the previous node if that id existed, else null
    */
   public OsmNode put(OsmNode node) {
+    created.add(node);
     return hmap.put(node, node);
   }
 
