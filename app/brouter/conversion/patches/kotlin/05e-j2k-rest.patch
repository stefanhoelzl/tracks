diff -ruN last/btools/codec/MicroCache2.kt cur/btools/codec/MicroCache2.kt
--- x/btools/codec/MicroCache2.kt	2026-09-14 19:43:51.127996828 +0200
+++ x/btools/codec/MicroCache2.kt	2026-09-14 19:47:18.455624561 +0200
@@ -30,7 +30,7 @@
     }
 
     fun readUnified(len: Int, u: IByteArrayUnifier): ByteArray? {
-        val b = u.unify(ab, aboffset, len)
+        val b = u.unify(ab!!, aboffset, len)
         aboffset += len
         return b
     }
diff -ruN last/btools/codec/TagValueCoder.kt cur/btools/codec/TagValueCoder.kt
--- x/btools/codec/TagValueCoder.kt	2026-09-14 19:43:51.127996828 +0200
+++ x/btools/codec/TagValueCoder.kt	2026-09-14 19:47:18.455624561 +0200
@@ -131,7 +131,7 @@
             res = validator.unify(buffer, 0, len)
         }
 
-        val accessType = if (validator == null) 2 else validator.accessType(res)
+        val accessType = if (validator == null) 2 else validator.accessType(res!!)
         if (accessType > 0) {
             val w = TagValueWrapper()
             w.data = res
diff -ruN last/btools/expressions/BExpressionContext.kt cur/btools/expressions/BExpressionContext.kt
--- x/btools/expressions/BExpressionContext.kt	2026-09-14 19:43:51.130720566 +0200
+++ x/btools/expressions/BExpressionContext.kt	2026-09-14 19:47:18.457986386 +0200
@@ -476,7 +476,7 @@
      * generate random values for regression testing
      */
     fun generateRandomValues(rnd: kotlin.random.Random): IntArray {
-        val data = createNewLookupData()
+        val data = createNewLookupData()!!
         data[0] = 2 * rnd.nextInt(2) // reverse-direction = 0 or 2
         for (inum in 1..<data.size) {
             val nvalues = lookupValues.get(inum)!!.size
@@ -831,7 +831,7 @@
     }
 
     @Throws(Exception::class)
-    private fun _parseFile(file: File, keyValues: MutableMap<String?, String>?): MutableList<BExpression?> {
+    private fun _parseFile(file: File, keyValues: MutableMap<String?, String?>?): MutableList<BExpression?> {
         _br = BufferedReader(FileReader(file))
         _readerDone = false
         val result: MutableList<BExpression?> = ArrayList<BExpression?>()
diff -ruN last/btools/mapaccess/MatchedWaypoint.kt cur/btools/mapaccess/MatchedWaypoint.kt
--- x/btools/mapaccess/MatchedWaypoint.kt	2026-09-14 19:43:51.133395470 +0200
+++ x/btools/mapaccess/MatchedWaypoint.kt	2026-09-14 19:47:18.460081388 +0200
@@ -36,7 +36,7 @@
     var directionDiff: Double = 361.0
 
     @JvmField
-    var wayNearest: MutableList<MatchedWaypoint?> = ArrayList<MatchedWaypoint?>()
+    var wayNearest: MutableList<MatchedWaypoint> = ArrayList<MatchedWaypoint>()
     @JvmField
     var hasUpdate: Boolean = false
 
diff -ruN last/btools/mapaccess/OsmFile.kt cur/btools/mapaccess/OsmFile.kt
--- x/btools/mapaccess/OsmFile.kt	2026-09-14 19:43:51.133395470 +0200
+++ x/btools/mapaccess/OsmFile.kt	2026-09-14 19:47:18.460081388 +0200
@@ -87,7 +87,7 @@
         ilon: Int,
         ilat: Int,
         dataBuffers: DataBuffers,
-        wayValidator: TagValueValidator,
+        wayValidator: TagValueValidator?,
         waypointMatcher: WaypointMatcher?,
         hollowNodes: OsmNodesMap?
     ): MicroCache? {
@@ -119,7 +119,7 @@
 
     @Throws(IOException::class)
     fun createMicroCache(
-        lonIdx: Int, latIdx: Int, dataBuffers: DataBuffers, wayValidator: TagValueValidator,
+        lonIdx: Int, latIdx: Int, dataBuffers: DataBuffers, wayValidator: TagValueValidator?,
         waypointMatcher: WaypointMatcher?, reallyDecode: Boolean, hollowNodes: OsmNodesMap?
     ): MicroCache? {
         val subIdx = (latIdx - divisor * latDegree) * divisor + (lonIdx - divisor * lonDegree)
diff -ruN last/btools/mapaccess/PhysicalFile.kt cur/btools/mapaccess/PhysicalFile.kt
--- x/btools/mapaccess/PhysicalFile.kt	2026-09-14 19:43:51.133395470 +0200
+++ x/btools/mapaccess/PhysicalFile.kt	2026-09-14 19:47:18.460081388 +0200
@@ -24,7 +24,7 @@
     @JvmField
     var fileHeaderCrcs: IntArray? = null
 
-    var creationTime: Long
+    var creationTime: Long = 0
 
     @JvmField
     var fileName: String?
diff -ruN last/btools/router/AreaInfo.kt cur/btools/router/AreaInfo.kt
--- x/btools/router/AreaInfo.kt	2026-09-14 19:43:51.135838778 +0200
+++ x/btools/router/AreaInfo.kt	2026-09-14 19:47:18.463002014 +0200
@@ -30,7 +30,7 @@
         val test = elevStart - elev
         if (abs(test) < 50) elev50++
 
-        val ld2 = expctxWay.createNewLookupData()
+        val ld2 = expctxWay.createNewLookupData()!!
         expctxWay.decode(ld2, false, ab)
 
         if (numForest != -1 && ld2[numForest] > 1) {
diff -ruN last/btools/router/RoutingEngine.kt cur/btools/router/RoutingEngine.kt
--- x/btools/router/RoutingEngine.kt	2026-09-14 19:43:51.136996800 +0200
+++ x/btools/router/RoutingEngine.kt	2026-09-14 19:47:18.463364317 +0200
@@ -208,7 +208,7 @@
             val messageList: MutableList<String?> = ArrayList<String?>()
             var i = 0
             while (true) {
-                track = findTrack(refTracks, lastTracks)
+                track = findTrack(refTracks, lastTracks)!!
 
                 // we are only looking for info
                 if (routingContext.ai != null) return
diff -ruN last/btools/util/CompactLongMap.kt cur/btools/util/CompactLongMap.kt
--- x/btools/util/CompactLongMap.kt	2026-09-14 19:43:51.139996791 +0200
+++ x/btools/util/CompactLongMap.kt	2026-09-14 19:47:18.466843463 +0200
@@ -130,7 +130,7 @@
         require(size != Int.MAX_VALUE) { "cannot grow beyond size Int.MAX_VALUE" }
 
         // put the new entry in the first array
-        al!![0][0] = id
+        al!![0]!![0] = id
 
         // determine the first empty array
         var bp = size++ // treat size as bitpattern
@@ -160,7 +160,7 @@
             for (i in 0..<idx) {
                 val p = pa[i]
                 if (p > 0) {
-                    val currentId = al!![i][p - 1]
+                    val currentId = al!![i]!![p - 1]
                     if (maxIdx < 0 || currentId > maxId) {
                         maxIdx = i
                         maxId = currentId
@@ -169,9 +169,9 @@
             }
 
             // current maximum found, copy to target array
-            require(!(n < al!![idx].size && maxId == al!![idx][n])) { "duplicate key found in late check: " + maxId }
+            require(!(n < al!![idx]!!.size && maxId == al!![idx]!![n])) { "duplicate key found in late check: " + maxId }
             --n
-            al!![idx][n] = maxId
+            al!![idx]!![n] = maxId
             vla!![idx]!![n] = vla!![maxIdx]!![pa[maxIdx] - 1]
 
             --pa[maxIdx]
@@ -218,7 +218,7 @@
 
     // does sorted array "a" contain "id" ?
     private fun contains(idx: Int, id: Long, doPut: Boolean): Boolean {
-        val a = al!![idx]
+        val a = al!![idx]!!
         var offset = a.size
         var n = 0
 
@@ -249,8 +249,8 @@
             while (bp != 0) {
                 if ((bp and 1) == 1) {
                     val p = pa[idx]
-                    if (p < al!![idx].size) {
-                        val currentId = al!![idx][p]
+                    if (p < al!![idx]!!.size) {
+                        val currentId = al!![idx]!![p]
                         if (minIdx < 0 || currentId < minId) {
                             minIdx = idx
                             minId = currentId
diff -ruN last/btools/util/CompactLongSet.kt cur/btools/util/CompactLongSet.kt
--- x/btools/util/CompactLongSet.kt	2026-09-14 19:43:51.139996791 +0200
+++ x/btools/util/CompactLongSet.kt	2026-09-14 19:47:18.466843463 +0200
@@ -50,7 +50,7 @@
         require(size != Int.MAX_VALUE) { "cannot grow beyond size Int.MAX_VALUE" }
 
         // put the new entry in the first array
-        al!![0][0] = id
+        al!![0]!![0] = id
 
         // determine the first empty array
         var bp = size++ // treat size as bitpattern
@@ -79,7 +79,7 @@
             for (i in 0..<idx) {
                 val p = pa[i]
                 if (p > 0) {
-                    val currentId = al!![i][p - 1]
+                    val currentId = al!![i]!![p - 1]
                     if (maxIdx < 0 || currentId > maxId) {
                         maxIdx = i
                         maxId = currentId
@@ -88,9 +88,9 @@
             }
 
             // current maximum found, copy to target array
-            require(!(n < al!![idx].size && maxId == al!![idx][n])) { "duplicate key found in late check: " + maxId }
+            require(!(n < al!![idx]!!.size && maxId == al!![idx]!![n])) { "duplicate key found in late check: " + maxId }
             --n
-            al!![idx][n] = maxId
+            al!![idx]!![n] = maxId
 
             --pa[maxIdx]
         }
@@ -125,7 +125,7 @@
 
     // does sorted array "a" contain "id" ?
     private fun contains(idx: Int, id: Long): Boolean {
-        val a = al!![idx]
+        val a = al!![idx]!!
         var offset = a.size
         var n = 0
 
@@ -151,8 +151,8 @@
             while (bp != 0) {
                 if ((bp and 1) == 1) {
                     val p = pa[idx]
-                    if (p < al!![idx].size) {
-                        val currentId = al!![idx][p]
+                    if (p < al!![idx]!!.size) {
+                        val currentId = al!![idx]!![p]
                         if (minIdx < 0 || currentId < minId) {
                             minIdx = idx
                             minId = currentId
diff -ruN last/btools/util/StringUtils.kt cur/btools/util/StringUtils.kt
--- x/btools/util/StringUtils.kt	2026-09-14 19:43:51.139996791 +0200
+++ x/btools/util/StringUtils.kt	2026-09-14 19:47:18.466843463 +0200
@@ -28,7 +28,7 @@
         return escape(s, xmlChr, xmlEsc)
     }
 
-    private fun escape(s: String, chr: CharArray, esc: Array<String?>): String {
+    private fun escape(s: String, chr: CharArray, esc: Array<String>): String {
         var sb: StringBuilder? = null
         for (i in 0..<s.length) {
             val c = s.get(i)
diff -ruN last/btools/util/TinyDenseLongMap.kt cur/btools/util/TinyDenseLongMap.kt
--- x/btools/util/TinyDenseLongMap.kt	2026-09-14 19:43:51.139996791 +0200
+++ x/btools/util/TinyDenseLongMap.kt	2026-09-14 19:47:18.466843463 +0200
@@ -71,7 +71,7 @@
         require(size != Int.MAX_VALUE) { "cannot grow beyond size Int.MAX_VALUE" }
 
         // put the new entry in the first array
-        al[0][0] = id
+        al[0]!![0] = id
 
         // determine the first empty array
         var bp = size++ // treat size as bitpattern
@@ -101,7 +101,7 @@
             for (i in 0..<idx) {
                 val p = pa[i]
                 if (p > 0) {
-                    val currentId = al[i][p - 1]
+                    val currentId = al[i]!![p - 1]
                     if (maxIdx < 0 || currentId > maxId) {
                         maxIdx = i
                         maxId = currentId
@@ -110,9 +110,9 @@
             }
 
             // current maximum found, copy to target array
-            require(!(n < al[idx].size && maxId == al[idx][n])) { "duplicate key found in late check: " + maxId }
+            require(!(n < al[idx]!!.size && maxId == al[idx]!![n])) { "duplicate key found in late check: " + maxId }
             --n
-            al[idx][n] = maxId
+            al[idx]!![n] = maxId
             vla[idx]!![n] = vla[maxIdx]!![pa[maxIdx] - 1]
 
             --pa[maxIdx]
@@ -149,7 +149,7 @@
 
     // does sorted array "a" contain "id" ?
     private fun contains(idx: Int, id: Long, rv: ByteArray?): Boolean {
-        val a = al[idx]
+        val a = al[idx]!!
         var offset = a.size
         var n = 0
 
